services:
  - type: web
    name: shopping-app
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar -Dspring.profiles.active=prod target/shopping-0.0.1-SNAPSHOT.jar
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: CORS_ALLOWED_ORIGINS
        value: http://localhost:3000,http://localhost:5173,https://your-react-app.vercel.app
      - key: JWT_SECRET
        sync: false  # Generate a strong secret - use: openssl rand -base64 32
      - key: RAZORPAY_KEY
        value: rzp_test_E1v7jL3XBuxjg0
      - key: RAZORPAY_SECRET
        value: CwvaDEvYSfJuP40WfrpyrxpH
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        value: ADI@28adi
      - key: GOOGLE_CLIENT_ID
        sync: false  # Set this in Render dashboard (optional)
      - key: GOOGLE_CLIENT_SECRET
        sync: false  # Set this in Render dashboard (optional)
      - key: GITHUB_CLIENT_ID
        value: Ov231iCogGkwyc6V02jL
      - key: GITHUB_CLIENT_SECRET
        sync: false  # Set this in Render dashboard (optional)
      - key: DATABASE_URL
        fromDatabase:
          name: shopping-db
          property: connectionString
