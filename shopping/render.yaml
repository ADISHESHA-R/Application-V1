# Render Deployment Configuration
# This file tells Render how to build and run your Spring Boot application

# Build Command
# Render will automatically detect this is a Maven project and run: mvn clean package

# Start Command  
# This tells Render how to start your application
java -jar target/Shopping-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod

# Environment Variables (set these in Render dashboard)
# DATABASE_URL=postgresql://username:password@host:port/database
# RAZORPAY_KEY=your_razorpay_key
# RAZORPAY_SECRET=your_razorpay_secret
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your_secure_password
